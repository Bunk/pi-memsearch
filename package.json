{
  "name": "pi-memsearch",
  "version": "0.0.1",
  "description": "A pi extension integrating the memsearch agent memory plugin (semantic, persistent memory backed by Markdown + Milvus).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "keywords": [
    "pi",
    "pi-extension",
    "pi-package",
    "memsearch",
    "memory",
    "milvus",
    "ai-agent"
  ],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  }
}
