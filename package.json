{
  "name": "pi-memsearch",
  "version": "0.1.0",
  "description": "Persistent, semantic long-term memory for the pi coding agent: automatic capture and cold-start recall, progressive-disclosure search, durable project/user notes, and procedural skills — backed by memsearch (Markdown + Milvus).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "keywords": [
    "pi",
    "pi-extension",
    "pi-package",
    "memsearch",
    "memory",
    "milvus",
    "ai-agent"
  ],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  }
}
